{
  "name": "root-and-fruit-frontend",
  "version": "1.0.0",
  "description": "Root & Fruit Integrity Index — Frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"(no tests yet)\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}