project_id = "root-and-fruit-dev"

# ⚠️ Firestore location is PERMANENT — you cannot change it after the first
# apply. This is also the Cloud Run region. Pick a region (e.g. "us-central1")
# or a multi-region ("nam5", "eur3"). Keep dev/staging/prod consistent.
region = "us-central1"
